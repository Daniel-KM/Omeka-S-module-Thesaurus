<?php
/**
 * @var \Zend\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\ItemRepresentation[] $data
 * @var array $options
 * @var int $level
 */

?>
<ul>
    <?php foreach ($data as $current): ?>
    <li><?= $current['self']->link($current['self']->displayTitle()) ?>
        <?php if (!empty($current['children'])): ?>
        <?= $this->partial('common/thesaurus-tree-recursive', ['data' => $current['children'], 'options' => $options, 'level' => $level + 1]) ?>
        <?php endif; ?>
   </li>
   <?php endforeach; ?>
</ul>
